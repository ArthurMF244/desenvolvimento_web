services:
  si-chamados:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: si-chamados
    ports:
      - "80:80"
    volumes:
      - ./:/usr/local/apache2/htdocs:ro
    restart: unless-stopped
